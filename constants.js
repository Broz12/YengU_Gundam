// centralized configuration and constant values
const API_BASE_URL = 'https://api.example.com';
const MAX_RETRIES = 5;
const TIMEOUT = 3000;

module.exports = {
    API_BASE_URL,
    MAX_RETRIES,
    TIMEOUT,
};